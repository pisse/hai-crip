/* global define */
require([
	'../scope',
	//'zepto',
	'../views',
	'swipe'
], function (scope, Views, swipe) {
	'use strict';

	console.log(scope);
	console.log(Views);
	console.log(swipe);
	return scope;
});
